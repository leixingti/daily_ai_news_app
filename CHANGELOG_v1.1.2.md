# 版本 1.1.2 更新说明

## 发布日期
2026-01-22

## 主要更新

### 🔧 修复国外新闻翻译问题

#### 问题描述
在之前的版本中，国外新闻的翻译功能存在以下问题：
- 只翻译了新闻摘要（description），标题（title）仍然是英文
- 翻译后的内容保存到 `content` 字段，但前端显示的 `summary` 字段是从原始英文截取的
- 导致用户在首页看到的国际新闻标题和摘要都是英文

#### 解决方案
1. **同时翻译标题和摘要**
   - 修改 `server/rssNewsCrawler.ts` 中的 RSS 解析逻辑
   - 国际新闻的标题和摘要都会通过 LLM 翻译成中文
   - 翻译后的内容直接保存到数据库的 `title` 和 `summary` 字段

2. **性能优化**
   - 使用 `Promise.all` 并行翻译标题和摘要
   - 减少翻译等待时间，提高爬虫效率

3. **错误处理**
   - 添加完善的 try-catch 错误处理
   - 翻译失败时使用原文，不影响爬虫继续运行
   - 添加详细的日志输出，便于调试和监控

#### 技术细节

**修改文件：** `server/rssNewsCrawler.ts`

**修改前：**
```typescript
if (region === "international") {
  description = await translateText(description);
}
```

**修改后：**
```typescript
if (region === "international") {
  try {
    // 并行翻译标题和描述以提高效率
    const [titleResult, descResult] = await Promise.all([
      translateText(title),
      translateText(description)
    ]);
    translatedTitle = titleResult;
    translatedDescription = descResult;
    console.log(`[RSSNewsCrawler] Translated: "${title.substring(0, 50)}..." -> "${translatedTitle.substring(0, 50)}..."`);
  } catch (error) {
    console.error(`[RSSNewsCrawler] Translation failed for item, using original text:`, error);
  }
}
```

## 影响范围

### 用户体验改进
- ✅ 国际新闻的标题和摘要都显示为中文
- ✅ 用户无需手动切换语言或查看原文
- ✅ 提升了国际新闻的可读性和用户体验

### 系统性能
- ✅ 并行翻译提高了爬虫效率
- ✅ 错误处理确保了系统稳定性
- ✅ 详细日志便于问题排查

## 部署说明

### 自动部署
如果您使用 Render 或其他 CI/CD 平台，推送到 GitHub 后会自动触发部署。

### 手动部署
```bash
# 拉取最新代码
git pull origin main

# 安装依赖
pnpm install

# 构建项目
pnpm build

# 启动服务
pnpm start
```

### 环境变量要求
确保以下环境变量已配置：
- `BUILT_IN_FORGE_API_KEY` - LLM API 密钥（用于翻译）
- `BUILT_IN_FORGE_API_URL` - LLM API 地址（默认：https://forge.manus.im）

## 验证方法

### 1. 检查日志
启动服务后，查看日志中是否有翻译记录：
```
[RSSNewsCrawler] Translated: "OpenAI Releases GPT-5..." -> "OpenAI 发布 GPT-5..."
```

### 2. 检查前端显示
访问网站首页，筛选"国际新闻"，确认标题和摘要都是中文。

### 3. 检查数据库
查询数据库中 `region = 'international'` 的新闻，确认 `title` 和 `summary` 字段是中文。

## 后续计划

- [ ] 添加翻译质量监控
- [ ] 支持更多语言的翻译
- [ ] 优化翻译缓存机制
- [ ] 添加翻译失败重试机制

## 相关链接

- GitHub 仓库：https://github.com/leixingti/daily_ai_news_app
- 提交记录：https://github.com/leixingti/daily_ai_news_app/commit/24a03e7
- 问题反馈：https://github.com/leixingti/daily_ai_news_app/issues

---

**感谢使用 AI 每日新闻应用！** 🚀
